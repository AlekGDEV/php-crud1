<div class="container-fluid mt-5 text-center">
    <h1>Listar Tabela</h1>
    <hr>
    <table class="table text-center">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Matricula</th>
                <th>Cidade</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody class="table-secondary table-stripped table-hover">
            <?php
                foreach ($alunos as $cadaAluno) {
                    $id = $cadaAluno['idalunos'];
                    echo "
                    <tr>
                        <td class='pt-3 fw-bold'>{$cadaAluno['idalunos']}</td>
                        <td class='pt-3'>{$cadaAluno['nome']}</td>
                        <td class='pt-3'>{$cadaAluno['matricula']}</td>
                        <td class='pt-3'>{$cadaAluno['cidade']}</td>
                        <td>
                            <a href='/listar?id={$id}' class='btn btn-warning me-2' data-bs-toggle='modal' data-bs-target='#editModal'>Editar</a>
                            <a href='/excluir?id={$id}' class='btn btn-danger text-white'>Excluir</a>
                        </td>
                    </tr>";
                }
            ?>
        </tbody>
    </table>
</div>
<div class="modal" id="editModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Atualizar informações do aluno:</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="" method="POST">
                <div class="modal-body">
                    <label for="nome">Editar nome: </label>
                    <input class="form-control bg-light mb-2" name="valor_nome" id="nome" type="text" value="<?php echo $aluno['nome']?>">
                    <label for="matricula">Editar matricula: </label>
                    <input class="form-control bg-light mb-2" name="valor_matricula" id="matricula" type="text" value="<?php echo $aluno['matricula']?>">
                    <label for="cidade">Editar cidade: </label>
                    <input class="form-control bg-light" name="valor_cidade" id="cidade" type="text" value="<?php echo $aluno['cidade']?>">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button name="submeter" class="btn btn-primary">Salvar alterações</button>
                </div>
            </form>
        </div>
    </div>
</div>